{
	"name": "msskolahrou-extensions",
	"version": "1.3.0",
	"type": "module",
	"scripts": {
		"build:css": "sass src/scss:assets/css --style=compressed --source-map",
		"watch:css": "sass src/scss:assets/css --watch --source-map",
		"build:zip": "node scripts/build-zip.js",
		"build": "npm run build:css && npm run build:zip"
	},
	"devDependencies": {
		"archiver": "^7.0.0",
		"sass": "^1.77.0"
	}
}
